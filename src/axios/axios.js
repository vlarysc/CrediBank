/* import axios from "axios";

export const baseUrl = axios.create({
  baseUrl: 'http://localhost:3000'
})

export const url = async (url, setDados) => {
  const res = await baseUrl.get(url);
  setDados(res.data)
} */